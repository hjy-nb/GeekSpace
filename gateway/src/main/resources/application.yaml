server:
  port: 4458

spring:
  application:
      name: gateway
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        username: nacos
        password: 695641275abcd
    gateway:
      routes:
          - id: auth
            uri: lb://auth
            predicates:
              - Path=/auth/**
            filters:
              - StripPrefix=1


jwt:
  secret_key: secret
